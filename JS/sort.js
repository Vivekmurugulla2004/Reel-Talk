document.addEventListener('DOMContentLoaded',()=>{const grid=document.querySelector('.Article_Card')||document.querySelector('.Review_Card');if(!grid)return;let bar=document.querySelector('.sort-bar');if(!bar){bar=document.createElement('div');bar.className='sort-bar';bar.innerHTML=`<label for="sortOrder">Sort:</label><select id="sortOrder"><option value="az">A \u2192 Z</option><option value="za">Z \u2192 A</option></select>`;grid.parentNode.insertBefore(bar,grid);}
function sortKey(card){const links=card.querySelectorAll('a');const titleLink=links[links.length-1];const raw=titleLink?titleLink.textContent:'';return raw.replace(/\s*\(\d{4}\)\s*$/,'').trim().toLowerCase().replace(/^(the|a|an)\s+/i,'');}
function sortCards(order){const cards=Array.from(grid.children).filter(c=>c.style.display!=='none');const hidden=Array.from(grid.children).filter(c=>c.style.display==='none');cards.sort((a,b)=>{const ta=sortKey(a);const tb=sortKey(b);return order==='az'?ta.localeCompare(tb):tb.localeCompare(ta);});grid.append(...cards,...hidden);}
document.getElementById('sortOrder').addEventListener('change',e=>{sortCards(e.target.value);});sortCards('az');const filterBtns=document.querySelectorAll('.filter-btn');if(!filterBtns.length)return;const classMap={playstation:'sony',xbox:'xbox',nintendo:'nintendo',dc:'dc',marvel:'marvel'};filterBtns.forEach(btn=>{btn.addEventListener('click',()=>{filterBtns.forEach(b=>b.classList.remove('active'));btn.classList.add('active');const filter=btn.dataset.filter;Array.from(grid.children).forEach(card=>{if(filter==='all'){card.style.display='';}else if(filter==='other'){card.style.display=(!card.classList.contains('dc')&&!card.classList.contains('marvel'))?'':'none';}else{const cls=classMap[filter]||filter;card.style.display=card.classList.contains(cls)?'':'none';}});sortCards(document.getElementById('sortOrder').value);});});});
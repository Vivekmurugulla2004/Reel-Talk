function renderFeed(items,opts){const grid=document.getElementById(opts.gridId||'review-grid');if(!grid)return;items.forEach(function(item){const card=document.createElement('div');card.className=opts.filterClass(item);card.innerHTML='<a class="cover-link" href="'+item.url+'"><img loading="lazy" src="'+item.poster+'" alt="'+item.title+' cover" width="225" height="337" onerror="rtImgFallback(this)"></a>'+
'<a href="'+item.url+'">'+item.title+'</a>';grid.appendChild(card);});}